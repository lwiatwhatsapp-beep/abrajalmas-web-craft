[build]
  command   = "npm run build"
  publish   = "dist/client"

[build.environment]
  NODE_VERSION  = "20"
  NITRO_PRESET  = "netlify"

[functions]
  directory            = "dist/server"
  node_bundler         = "none"
  included_files       = ["dist/server/**"]
